<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to t('.title'), root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown">
            <%= link_to t('.my_cards'), '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' %>
            <ul class="dropdown-menu">
              <li><%= link_to t('.decks'), decks_path %></li>
              <li><%= link_to t('.add_deck'), new_deck_path %></li>
              <li><%= link_to t('.cards'), cards_path %></li>
              <li><%= link_to t('.add_card'), new_card_path %></li>
            </ul>
          </li>
          <li class="dropdown">
            <%= link_to "#{current_user.email}", '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' %>
            <ul class="dropdown-menu">
              <li><%= link_to t('.user_edit'), edit_dashboard_user_path(current_user) %></li>
              <li><%= link_to t('.exit'), dashboard_logout_path, method: :post %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('.lang_en'), home_login_path(locale: 'en') %></li>
          <li><%= link_to t('.lang_ru'), home_login_path(locale: 'ru') %></li>
          <li><%= link_to t('.sign_up'), home_signup_path %></li>
          <li><%= link_to t('.login'), home_login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
